describe('App tests', () => {
  it('return true to test how jest is awesome', () => {
    const jestIsAwesome = true;
    expect(jestIsAwesome).toEqual(true);
  });

  it('return true to test watch mode', () => {
    const watchModeIsAwesome = true;
    expect(watchModeIsAwesome).toEqual(true);
  });
});
